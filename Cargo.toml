[package]
name = "efr32-rail"
version = "0.1.1"
edition = "2024"
build = "build.rs"

[lib]
name = "efr32_rail"

[[bin]]
name = "trx_demo"
required-features = ["defmt-logging"]

[dependencies]
cortex-m = { version = "0.7.7", features = ["critical-section-single-core"] }
cortex-m-rt = { version = "0.7.5", features = ["device"] }
# critical section feature is needed to call `Peripherals::take`
# rt is needed to be able to listen to interrupts
efr32mg22-pac = { git = "https://github.com/Bnyro/efr32mg22-pac", rev = "v0.1.1", features = [
  "critical-section",
  "rt",
] }

defmt = { version = "1.0", optional = true }
defmt-rtt = { version = "1.0", optional = true }

[build-dependencies]
bindgen = "0.72"
cc = "1.2"

[features]
# set logging levels here
default = []
defmt-logging = ["defmt", "defmt-rtt", "defmt-default"]

defmt-debug = []
# do NOT modify these features
defmt-default = []
defmt-error = []
defmt-info = []
defmt-trace = []
defmt-warn = []
